action_when_click = function() {
	//edit action in obj creation code
}

//var init
if !variable_instance_exists(id, "text") {
	text = "default";
}
selected = false;

//if the viewport and window has a different ratio
if view_visible[0] {
	view_scale = window_get_width() / camera_get_view_width(view_camera[0]);
}
else {
	view_scale = 1;
}

draw_obj = function() {
	
}